# Graph RAG PoC — 지식 문서 기반 3-트랙 RAG

위키 형태의 지식 문서를 질의응답으로 활용하기 위해 **Vector / Graph / Hybrid** 세 가지 RAG 방식을
직접 구현·비교한 개인 PoC. 실데이터를 안전하게 다루기 위한 **민감정보 마스킹 · 회귀 평가 하네스 · 엔티티 정규화**까지 포함한다.

> 개인 프로젝트 — 설계·구현·검증 전담

---

## 한 줄 요약
- 지식 문서를 대상으로 Vector·Graph·Hybrid **3가지 RAG를 직접 구현·비교**하고, 실데이터 투입을 위한 **민감정보 마스킹·결정론 평가·엔티티 정규화**까지 설계.
- **Neo4j 지식그래프 기반 Graph RAG**를 구축하고, 엔티티 진입을 키워드 매칭 → **임베딩 기반 Entity Linking**으로 개선; 하이브리드로 **다문서 종합 답변** 구현.

## 문제 / 목표
- 위키 지식을 질의응답으로 활용하는 RAG를 학습·검증.
- 단순 벡터 검색을 넘어 **Graph RAG(엔티티·관계 탐색)** 를 직접 구현해 방식별 강·약점을 이해.
- 실제 문서를 넣어도 **민감정보가 새지 않게** 처리하는 수준까지 확인.

## 파이프라인
```
문서 ─(민감정보 마스킹)─┬─ 청킹 → 임베딩 ───────────────→ Qdrant   (Vector)
                        └─ LLM 엔티티·관계 추출 → 노드+임베딩 → Neo4j   (Graph)

질문 ─┬─ Vector : 의미 유사 청크 검색 ───────────────→ LLM 답변(+근거)
      ├─ Graph  : 엔티티 매칭 → 1~2홉 관계 탐색 ──────→ LLM 답변(+근거)
      └─ Hybrid : ①Vector 검색 → ②그 맥락으로 Graph 진입·확장 → ③종합
```

## 구현한 것

### 1. 3-트랙 RAG
- **Vector**: Qdrant + LangChain(`QdrantVectorStore` · retriever · LCEL 체인)
- **Graph**: Neo4j + LLM(google-genai) — 문서에서 엔티티/관계를 추출→적재→관계 탐색
- **Hybrid**: Vector→Graph 순차 결합 — 벡터 검색 맥락으로 그래프 진입 보강, 두 근거 종합
  - *의도적으로 Vector는 LangChain, Graph는 raw SDK로 구현해 프레임워크 유무 차이도 학습*

### 2. Graph 엔티티 진입(Entity Linking) 개선
- 초기: 질문↔노드를 `CONTAINS` **키워드 부분일치** → 표현이 조금만 달라도 엉뚱한 노드
- 개선: **LLM 엔티티 추출 → 임베딩 코사인 매칭**(표준 Entity Linking) + lexical fallback

### 3. 엔티티 정규화 — 약어/동의어 사전
- `k8s = Kubernetes`처럼 같은 개념의 다른 표기를 통일해 **그래프 노드 분산 방지**
- **공통 기술용어 사전 + 도메인 고유약어 사전** 분리, **반자동 후보탐지 + 사람 검토** 워크플로우

### 4. 민감정보 마스킹 (실데이터 안전장치)
- 문서에는 개인정보·시크릿·내부 인프라 정보가 섞일 수 있으므로, **적재 시점에 결정론 정규식으로 마스킹**
  (email·전화·IPv4·비밀번호 플래그·`key=secret`·DB URL 자격증명·JWT 등) → 벡터DB·그래프·외부 LLM으로 **원문이 나가지 않게** 함
- 다중 방어: 민감 문서 선택 제외(1차) + 마스킹(2차) + 버전관리 미추적(3차)

### 5. 평가 하네스
- **골든셋(질문+기대결과) + 순수 채점 함수**(`source_recall`/`entity_recall`/`keyword_coverage`/`negative_correct`)
- **데이터셋·채점은 무외부호출·결정론**으로 분리 → 데이터/코드 변경 시 회귀 비교. 부정 케이스로 **환각 여부**도 채점

### 6. 운영성 개선
- 임베딩 **배치 호출**로 무료 티어 rate limit 회피 + 429 재시도
- 여러 트랙 웹을 **탭 하나로 통합**하고, 지식그래프를 **인터랙티브로 시각화**(vis-network)하는 탐색 화면 추가

## 핵심 설계 결정 (어떤 생각을 했나)
- **그래프 진입이 승부처**: 관계가 잘 만들어져도 "들어가는 문(entity linking)"이 부실하면 소용없다 → 키워드→의미(임베딩) 매칭으로 교체
- **하이브리드의 근거**: 벡터의 의미검색이 그래프의 진입 약점을 보완한다는 가설로 Vector→Graph 순차 설계
- **마스킹의 위치**: 보안 경계를 "적재 시점"에 둬서 어떤 트랙을 타든 원문이 밖(DB·LLM·버전관리)으로 안 나가게
- **정규화 전략**: 세상이 아는 용어는 LLM/공통사전, **도메인 고유약어는 사람이 채우는 사전**으로 역할 분담. 동음이의(예: CM=ConfigMap/Change Mgmt)는 자동 통합 금지
- **평가 철학**: LLM 답변은 비결정적이라 "좋은가"보다 **"안 깨졌나(회귀)"** 를 재는 결정론 채점을 우선
- **제약을 상수로**: 무료 LLM 쿼터(생성 하루 20회, 임베딩 분당 100회)를 전제로 배치·검색전용·재시도 설계

## 문제 해결 사례 (엔지니어링 판단)
- **엔티티 진입 개선**: 표현이 조금 다른 질문에 키워드 매칭이 엉뚱한 노드를 잡던 것을, 임베딩 Entity Linking으로 바꿔 표현 차이에 강건해짐
- **보안 규율**: 실 문서에는 민감정보가 섞일 수 있어 마스킹 파이프라인을 먼저 구축. 테스트 픽스처는 **전부 더미값으로 관리**해 실제 값이 저장소에 유입되지 않도록 커밋 게이트 운용
- **마스킹 오탐 수정**: 정규식이 10자리 문서 ID를 전화번호로 오인 → **숫자 경계 lookaround로 수정 + 회귀 테스트** 추가
- **쿼터 한계 대응**: 임베딩 분당 요청 제한에 걸려 **배치 호출 + 429 재시도**로 전환해 대량 적재 성공

## 제품화 로드맵 설계 (PoC 너머)
- 실서비스 전환 시 필요한 구성요소 정리: 수집·증분 동기화(ETL), 문서 권한(ACL) 필터, LLM 쿼터 게이트웨이·온프렘 LLM 전환, 평가 골든셋 상시화 등
- 의존관계 기반 **P0~P3 단계**로 순서 도출(신뢰 기반 → 지속가능 → 관계 고도화 → 완성)

## 결과물
- 3-트랙 RAG 동작(질의 웹 + CLI), **Hybrid가 여러 문서를 가로질러 종합 답변** 확인
- 지식그래프 **157 엔티티 / 176 관계**, 벡터 **31 청크** 규모로 적재·질의
- **결정론 테스트 43개**(마스킹·평가·정규화), 골든셋 6케이스
- 마스킹 후 **잔존 민감정보(개인정보·시크릿·내부 IP) 스캔 0** 검증
- 근거 없는 항목엔 **환각 대신 "확인할 수 없음"** 으로 응답(근거 기반)

## 기술 스택
Python · LangChain · Qdrant(Vector DB) · Neo4j(Graph DB) · LLM(임베딩·생성) · FastAPI · pytest · vis-network

## 한계 / 범위 (PoC)
- 소규모 코퍼스 수준 — 정확도는 정성 확인 + 소형 골든셋 기준
- 문서 수집 자동화, 권한(ACL) 필터, 온프렘 LLM 전환은 미구현(로드맵으로 정리)
- 무료 LLM 쿼터 제약으로 대규모 정량 평가는 제한

## 배운 점
- Graph RAG의 실질 병목은 관계 추출보다 **엔티티 진입/정규화**라는 점
- Vector와 Graph는 검색 방식뿐 아니라 **추상화 수준(프레임워크 사용 유무)** 도 달라, 하이브리드 비교 시 변수 혼입에 유의
- 실데이터를 다루는 순간 **마스킹·평가 같은 "주변 장치"가 핵심 파이프라인만큼 중요**해진다는 것
